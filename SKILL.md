---
name: asktipitaka
description: "用中文向巴利三藏提问，给出带可回查坐标的答案。当使用者用中文问一个佛法、经文、律典、巴利术语、人物、地点或典故的问题，而答案应当出自巴利三藏原典而不是模型记忆时，使用本 skill。它把中文问题转成该检索的巴利词，按固定配方取材料，再依据材料写答案，每条引用附 [书号:段号] 坐标。不要用它做不需要回查原典的闲聊。"
---

# 问藏 · 用中文问巴利三藏

**一句话：用中文问，去巴利三藏原典里找答案，每句都带可回查的坐标。**

数据库来自 **WikiPali** 网站，检索功能建立在 **wikipali research** 的基础上
（`install.sh` 会自动取好）。在这个基础上，本 skill 接上中文这一头：

1. **中文问题 → 该查哪个巴利词**；
2. **一套取数配方**——每步取多少、什么时候往后连读，都有定数。

---

## 开始之前：确认装好了

**检索用的是 `bin/wp`**（本包目录下），它会自己找到 WikiPali 检索 CLI。

```bash
bin/wp forms parivāsa      # 能出词形列表就是好的
```

若报「找不到 WikiPali 检索 CLI」，**跑一次安装即可，不用问使用者**：

```bash
bash install.sh
```

它会把底座取好并自检。**没有别的东西要装**——不需要登录、不需要 `pip install`、
不需要插件市场。本包自己只用 Python 标准库。

⚠️ 下面所有命令都写成 `bin/wp …`。**别改成 `wikipali …`** —— 使用者机器上
那个命令未必在 PATH 上，`bin/wp` 才是统一入口。

**每次会话开头跑一次**（只跑一次，⛔ 不要每次提问都跑）：

```bash
bin/check-update
```

**有新版它会自己装上**，你什么都不用做，也不用问使用者——
更新完它会打印一行「已自动更新」，把那一行转告使用者就行。
没有新版、或者离线，它什么都不打印，直接往下走。

⚠️ **为什么是自动装、不是问一句**：使用者多数是跟你说话，不是敲终端。
「提示他自己去跑 `install.sh`」＝ **他会永远停在旧版**。
唯一不自动装的情况是**本地改过文件**——那时它会说一声并列出改了哪些，
⛔ 那种情况**别替使用者决定**，把话转告他就行。

---

## ⛔ 铁律：违反了就是给出假答案

这八条不是建议。**每一条后面都有一次真实的翻车。**

1. **每一条写进答案的引用，必须带得回坐标。** 手里没有坐标的内容，
   一个字都不许写进答案。宁可说「本次检索没有找到」，也不要凭印象转述。

2. **检索前必须先展开词形**（`bin/wp forms`，或给 `search --lemma`）。
   直接拿词典形去搜会 **返回 0 条且不报错**——这是最容易犯的错，
   因为它看起来像「搜过了，没有」。

3. **0 条结果不等于「没有材料」。** 依次怀疑：
   词形没展开 → 词根选错 → **拼法错了**（尤其是变音符号：`bhīsakka` 全语料 1 句、
   `bhisakka` 42 句，差一个长音符整题就废）→ 范围限太窄 → 才是真的没有。
   **把怀疑过程说给使用者听。**

4. **「我没有检索到」不等于「不存在」。** 要否定某部经、某个说法的存在，
   只能说「本次检索没有找到」，不能说「三藏中没有」。
   （曾把《花经》答成「没有这部经」，而它确在相应部·蕴品。）

5. **每条引用照实标出层次：根本／义注／复注／藏外。**
   标注必须准确；判断不出层次时写「层次未标注」，**绝不默认「根本」**。

6. ⛔ **绝不拿「是不是佛说」给材料分高下。** 见下方「层次的口径」一节——
   这一条是教理红线，不是文风偏好。

7. **判断依据是巴利原文**，译文只作佐证。

8. **书名一律查表，不要自己翻译书名**（顺序见「C · 依据材料写答案」那一节）。

---

## 流程

### A · 中文问题 → 该查哪几个巴利词

```bash
python3 lib/pickwords.py "使用者的问题原文" --json
```

它拿一张 **17059 条的权威译名表**捞候选，靠三道判据：
二字组合重叠（含稀有度捷径）＋ 中译精确匹配 ＋ **单字术语**。
⚠️ 输出是**候选，不是结论**。

输出可能有**两块**，第二块要区别对待：

```
候选（越靠前越可能是这题要查的；最终选哪几个由你定，最多 4 个）：
  tipiṭaka        三藏
  sīlasaṃvara     戒律

单字术语（另一类，**可能跟这道题不相干，自己判断**）：
  lasuṇa          蒜
```

⚠️ **第二块是「中译只有一个汉字」的术语**（蒜、疑、衣、触、信、灭…）。
它们靠字面碰上，**命中不代表相干**——但它们**只有这一条路能进**，
上面两道判据完全够不着。问「大蒜的戒律」时 `lasuṇa` 就出在这一块。

**你要做的**：读候选，结合问题本身，**自己定最终检索词，最多 4 个**。

- 多于 4 个会发散，上下文也装不下；
- **专名优先选原词，不要选它的解释词**——问「荤腥外道」要查 `āmagandha`，
  不要拆成 `āmaka`（生）＋`maṃsa`（肉）；
- 候选为空**不代表查不了**，那只说明表里没有这个中译，
  这时按你自己的巴利知识定词，但铁律 3 更要当心。

⚠️ **A 一出错就是硬伤**：材料根本没拿回来，后面全白搭。
实测这一层出错的题里，**七成以上是彻底答砸**，不是「答得不够好」。

### B · 拿词去取材料

**每一步的上限都是量出来的，别自行放大。**

### ⚡ 先说最要紧的一条：**用 `bin/gather`，别一步步敲**

```bash
bin/gather <词> <词> …          # 词形＋分层分布＋检索，一次全取回来
```

它把每个词的三个接口、以及多个词之间，**全部并发**发出去，一次吐一份摘要。

⚠️ **一条条敲会慢很多**——每敲一条都要停下来读一遍、想一遍。用这一条。

**取回来之后再按坐标取原文**：

```bash
bin/wp get <坐标> <坐标> …      # 缺省给巴利原文
```

需要单独跑某一个接口时（复查、翻页、换收窄条件），还是用 `bin/wp`：

```bash
bin/wp forms <词>                      # 铁律 2：永远的第一步
bin/wp dist --lemma <词>               # 分层分布
bin/wp search --lemma <词> --limit 40  # 一次 40 条
bin/wp related <坐标>                  # 本文 ↔ 注疏对查
```

| 规矩 | 数 | 为什么是这个数 |
| --- | --- | --- |
| 一次检索要多少条 | **40** | 要 8 条还是 40 条**代价一样**，所以一次要够；后面的取舍花的是你自己的上下文 |
| 低频词取几段 | **全取** | 语料里 ≤40 条的词很精准，砍它是自伤 |
| 高频词取几段 | **前 8** | >40 条的词多半是泛词，取多了是噪声 |
| 所有词合计 | **≤48 段** | 上下文的闸门 |
| 往后连读 | **≤20 段** | 见下 |

**先看 `dist` 再决定**：命中集中在律藏就用 `--tags vinaya` 收窄；
本文命中少而义注多，说明这是个**注释书概念**，答案结构要跟着调整。
⚠️ `dist` 数的是**词次**，`search` 数的是**段落数**，两个数不相等，别写混。

⚠️⚠️ **`dist` 打印的 `--book` 数字，跟坐标里的书号不是同一套。**
实测：`dist` 显示「(DN) Mahāvaggaṭṭhakathā `--book 121`」，
但坐标空间里 **121 是 Nettippakaraṇa-aṭṭhakathā**，长部大品义注在坐标里是 **104**。
**收窄请用 `--tags`（mūla／aṭṭhakathā／ṭīkā／vinaya…），别照抄 `--book` 的数字。**

**命中太多、40 条里没找到想要的，就翻页**：

```bash
bin/wp search --lemma <词> --limit 40 --offset 40    # 第二页
```

⚠️ 高频词（几百上千段命中）常常要翻两三页才见到定义段——
**「一次 40 条」是每页的量，不是总量上限。**

**要汉译原文的话**（`get` 默认只给巴利）：

先查该段有哪些译本通道，拿到中文那个的 uid，再指定它取：

```bash
bin/wp versions <坐标>              # 看这一段有哪些译本
bin/wp get <坐标> --channel <uid>   # 取指定译本
```

⚠️ **别猜 channel 名**——猜错一律 HTTP 500。
⚠️ 而且**很多段落根本没有汉译**（实测 121:279 就只有巴利、逐词、英译义注、缅文口义）。
没有就如实说没有。**判断依据始终是巴利原文，汉译只作佐证（铁律 7）。**

**往后连读**（这一步很容易漏，漏了就只拿到空壳）：
检索命中的常常是**标题**，正文在后面。所以要从命中处往后再读几段。

- 起点＝片段短的那几条（像标题的），**只看每个词的前 8 条，最多 2 个起点**；
- 读多少：**列举类 12 段／找某部经 10 段／故事 20 段**，合计 ≤20 段；
- **「某词什么意思」这类定义题不用连读。**

**层次不齐时的兜底**（`bin/wp related <坐标>`）：

- 一条**根本**都没取到 → 从义注／复注反查它注释的经文；
- 一条**义注／复注**都没取到 → 从根本反查它的注疏。

⚠️ **保持「兜底」，不要做成每题必做。** 实测把它改成必做项之后，
补进来的材料只有 **5.9%** 真被引用，而答案里的坐标总数反而**下降 17.9%**——
塞进去的注疏原文挤掉了真正有用的材料。**两次上线，两次撤回。**

### C · 依据材料写答案

- **只用手里的材料写**，每条引用附 `[书号:段号]` 坐标与书名、层次；

**书名与层次按这个顺序取，别跳级**：

| 顺序 | 来源 | 说明 |
| --- | --- | --- |
| 1 | **实时输出自带的** | `search`／`get`／`dist` 的结果里就有书名与 tags。**这是最可靠的**，因为它是活的 |
| 2 | `data/book_layers.json` | 本包自带的层次快照。实时输出没给 tag 时用 |
| 3 | `data/book_titles.json` | 本包自带的中文标注。实时输出只给巴利名，中文靠它 |
| 4 | `lib/booknames.py` | 从巴利书名推中文标注与层次的启发式，**给表里没有的新书兜底** |

```bash
python3 -c "import sys;sys.path.insert(0,'lib');import booknames as b;print(b.chinese_label('Kaṅkhāvitaraṇī-abhinavaṭīkā'))"
# → 疑惑度脱·新复注
```

⚠️ **`data/` 里那两张表是本包自带的旧快照，跟实时输出对不上时，一律以实时输出为准。**

快照过期是必然的——它是某个时点导出的静态文件，而语料是活的、会增删修订。
所以书名与层次的第一来源**永远是 `search`／`get`／`dist` 返回里自带的**，
静态表只在实时输出没给的时候补位。

⚠️ 另有一个容易糊涂的地方：**「义注」两个字既可能是书名的一部分，也可能是层次**。
例如某段标注是「义注」，而它的巴利书名是 `Nettippakaraṇa-aṭṭhakathā`——
那个「义注」说的是**层次**，不是书名。写答案时把两者分开写，
别让读者以为书就叫《义注》。

⚠️ `lib/booknames.py` 认不出时会返回「**层次未标注**」——
**照实写，绝不默认「根本」。认不出层次是小事，认错层次是大事。**

**材料不够的时候**：

- **材料不足就直说**，并给出改问的方向。
  ⛔ 不要为了「答得上」去放松这一条——那不是答得好，那是学会了编。
- 若判断材料确实不足，**可以补选上一轮没查过的词，回到 A 再走一轮，但只补一轮。**

### 收尾 · 反问一句

答完之后问使用者一句，是否要就这个问题继续追问。
上一轮查到的材料还在，接着问通常比重新问一遍更准。

**如果使用者表示答得不对、不满意，或者你自己觉得这次答得不好**，告诉他一句：

> 这个 skill 收不到任何自动反馈，作者看不到你问了什么。
> 如果愿意，把问题和答案发到 **feedback@asktipitaka.org**，或在仓库提 issue，
> 下一版会照着改。

⚠️ 这一句很要紧：**这个 skill 跑在使用者自己机器上，作者收不到任何使用数据**——
哪里答砸了、哪个词选错了，除非有人主动说，否则永远不会有人知道。
你是唯一能提醒他的人。

---

## 层次的口径 ⛔ 教理红线

标注层次是为了**标明出处类型**，不是给材料排高低。四条禁令：

1. ⛔ **禁止把「根本」等同于「佛亲口说」。** 根本里也有大弟子所说、结集所编。
2. ⛔ **禁止把义注、复注说成「非佛说」「后世的」「不是佛陀教说」。**
   佛说分广义与狭义，**义注复注属于广义佛说**，且是大弟子们整理的。
3. ⛔ **禁止任何分裂佛语的倾向性表达。**
4. ⛔ **不必向使用者解释「义注是什么」**，标出处即可。

**只摆事实**：给书名、给层次、给坐标，让使用者自己回查。

---

## ⚠️ 1.0 版没有的东西：交付前核对

问藏的线上版本在给出答案之前，还有一道**由代码执行**的核对——
逐条验证引用是否真的出自那一段，核不过的坐标直接摘掉。

**1.0 版没有这一道。** 这意味着：

> **本版给出的坐标，没有经过机器核验。**
> 引用之前请自己点开坐标核对一遍，尤其是**经名、人物、故事出处**这三类。

为什么要专门说这件事：那道核对之所以有用，**正因为它是代码，不是叮嘱**。
模型写答案，错误是概率性的——偶尔错一条是可容忍的噪声；
代码做核对，结果是确定性的——**要么全过，要么全拦**。
把它写成「请你核对一下」，它就退化成又一个概率性步骤，保证就没了。

所以本文不假装 1.0 有这个能力。**它会在后续版本里以脚本形式补上。**

⚠️ 另外两处线上有、1.0 没有的：**变音符号的机械纠错**（铁律 3 里那类拼写错，
线上是从语料反查改正的，本版只能靠你自觉）和**跨词交集**。

---

## 常见错误对照

| 现象 | 真正的原因 |
| --- | --- |
| 检索 0 条 | 多半是没展开词形，或拼法差一个变音符号 |
| 查定义时结果全是义注、复注 | **正常**，大部分名词解释在注疏里。但别据此断定本文没有 |
| 查案例时找不到本文 | 这才是问题。用 `dist` 的 `--tags mūla` 收窄再搜。⚠️ **别用 `dist` 打印的 `--book` 数字**，见下方「两套书号」 |
| 检索命中了却答不出内容 | 命中的是标题，没有往后连读 |
| 某段落取不到译文 | 该 channel 在该段落没有内容，如实报告 |
| `get` 报 500 | 忘了 channel。手动传参数时要确保 channel 在内 |
